import React, { FC } from 'react';
import { StyledAddButton } from './AddButton.styles';

const AddButton: FC = () => (
  <StyledAddButton>
    <button>I do nothing yet</button>
  </StyledAddButton>
);

export default AddButton;
